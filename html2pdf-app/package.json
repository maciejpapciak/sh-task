{
  "name": "html-to-pdf",
  "version": "1.0.0",
  "description": "Minimal Node app: convert static HTML to PDF using Puppeteer",
  "type": "module",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "pdf": "tsc && node dist/index.js"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}